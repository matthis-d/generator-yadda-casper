language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-casper-yadda' ]; then cd .. && eval "mv $currentfolder generator-casper-yadda" && cd generator-casper-yadda; fi

